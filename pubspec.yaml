name: linkedin_auto_poster
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0
  flutter_dotenv: ^6.0.0
  google_generative_ai: ^0.4.4
  intl: ^0.19.0
  url_launcher: ^6.2.2
  signin_with_linkedin: ^2.0.0
  firebase_auth: ^5.3.3
  firebase_core: ^3.8.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
